# Result interpretation and visual completion

Candidate based on `b2c0dc0d1f197f4b39d49572136331fb7a3f228a`, after PR56 merged. The original checkout, main and unrelated open PRs are untouched. One branch: `codex/result-interpretation-completion`. No merge or manual deployment.

[Visual contact sheet](contact-sheet.png) · [Synthetic review links](walkthrough.html) · [Six substantive before/after examples](copy-examples.md) · [Recomputed example inputs](copy-examples.json)

## Review the assembled result

Start with the two Precautionary Steward links in the walkthrough. Their complete synthetic responses produce the same named category but different interpretations and worked cases. Then inspect Foundation, Security, Technology and the Chinese Foundation rendering. Open the grouped Full analysis sections, read the rival before the favorable bibliography, and follow a source or reference link. The Atlas contains all six canonical marks at equal size and stroke weight.

The opening now combines recorded positions, applies that reasoning to specified facts, supplies a serious objection and names a condition that would change the argument. Status and scope follow the first interpretation. Foundation retains its exact nearby-reading qualification and secondary orientation matrix. AI comparison terms come from the two frozen weight vectors, with a separate rounding residual; the associated case question follows their actual contrast. The prior category wording remains explicitly qualified in disclosure.

## What the measurements establish

The before/after JSON files trace the containing-block chain and every fingerprint row. At 1440 and 1920 CSS pixels, the AI article was 130px left of the viewport center; its final offset is zero. At 1440, the baseline Atlas bars drifted by up to 13.844px between rows. At 390 their widths differed by up to 21.375px. Final shared tracks have zero row-origin and width drift in these captures.

Browser assertions cover 320, 390, 719, 720, 721, 768, 1024, 1440 and 1920px, six marks, row/label boundaries and canvas centers. Explicit 200% text enlargement at 768px opens all disclosures on AI, Atlas, Foundation, Chinese Foundation and Technology. This is text enlargement, not a claim of testing every browser zoom mode or a physical Safari device. Foundation's two-column chapters intentionally anchor copy on the left and the companion visual on the right.

The inherited [601-row surface ledger](../decision-exercises-release/surface-coverage.csv), [reading delta](../batch-b-reading-and-localization/reading-delta.json), [module journeys](../module-journey/README.md) and [access experience](../access-analytical-experience/README.md) remain the authorities. [route-delta.json](route-delta.json) identifies the same 162 finite URL targets; [finite-route-results.json](finite-route-results.json) records fresh-state renders, headings and internal links. This does **not** mean every page or embedded record was visually read. [inspection.csv](inspection.csv) separately records capture, visual inspection, geometry and executed tasks. Infinite payload/state space is represented by existing and added synthetic fixtures.

## Evidence and applicability

- **Direct observations:** Foundation retains its existing exact-bound local evidence. Security and Technology display selections from their registered payload banks. Fresh AI completion hands off exact form wording and choices only through the explicit review-to-result action. All three score-identical geopolitics pairs—7/7, 1/1 and 4/4—retain their different observations during that navigation. Reload and ordinary shared links receive score-based interpretation only. A matching saved draft is never treated as proof of which completion produced a link.
- **Model interpretation:** AI uses the resolved registered scorer's positions. Foundation uses resolved dimensions while preserving exact version-specific family contributions. Module prose receives the resolved bank, scorer and mode and reuses the existing form cuts for banks 3–5. Legacy bank2 has no per-axis calibration table; its prose explicitly describes the 1–7 scale without importing later calibration. Prose bands never change a result, threshold or identity.
- **Illustrative application:** Every worked case states its hypothetical facts, an applicable argument, a serious rival and a change condition. It is neither an answer transcript nor a behavioral prediction. Low or middle `humanFuture` cannot identify a preferred future.
- **Follow-up:** Questions expose unresolved choices. They create no answer, rescore, request or stored observation.

Known aggregation loss and clipping/order witnesses from PR51–54 remain unresolved and tested. Richer prose does not repair the model. The two existing geometric-compromise findings in Security and Technology remain finite, non-blocking editorial review items; no issued options or signals were changed.

## Protection exceptions

The newer owner brief narrowly supersedes the PR52/Batch B presentation boundary for live result interpretation, AI marks and disclosure structure. Canonical names, keys and source records remain unchanged. No issued bank, clarification, option order, scoring/calibration implementation, payload codec, local storage schema/key, consent or telemetry contract, dependency, map or existing Foundation sigil definition changed. The one review-screen addition supplies ephemeral AI evidence; the existing persisted snapshots and their fields are unchanged.

The current evidence writer refreshes only `artifacts/evidence/current-summary.{md,json}`. Audit rules, historical numeric evidence and baselines are preserved. Existing browser assertions are updated for the new module summaries and explicit matrix disclosure. The print budget allows the added worked application within five pages. A nonleading saved AI label keeps its necessary qualification even when that pushes the hero action below the first mobile viewport. The two changed Chinese screenshots use new result-interpretation names; historical snapshots remain intact.

## Validation and limitations

See [validation.md](validation.md) for exact commands and outcomes. Initial failures exposed the Foundation domain-card overflow at doubled text, applicability errors in old AI/Chinese prose, and overly broad raw-score bands for modules. These were corrected. Concurrent local browser suites also caused transient proxy timeouts; final browser runs are sequential. The visual detector has no layout findings, which is mechanical evidence rather than proof of editorial quality. An independent source reviewer confirmed the bounded interpretation, tuple dispatch and qualification fixes; no user study or psychometric validation is claimed.

All committed records and screenshots are synthetic. The Chinese example is a rendering of synthetic reported positions, not an assertion that English-issued responses were collected under Chinese item wording.
