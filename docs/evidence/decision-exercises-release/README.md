# Decision exercises and Foundation administration — Batch A

Production candidate, 7 September 2026. **No merge, production deployment initiated or new collection.** The owner’s adopted production-conversion brief and whole-product addendum authorize this narrow freeze exception. Batch B follows only after A is accepted and merged; this is not a restart of the former roadmap.

Base: `da96b7e9b51426ad2bfb0ab0cc9d0ad4bf248a79`. PR51 merged on 6 September; integrated content matches reviewed head `38ff7c796bd93dd0312fdf2c2ee32d76061bec0c`. Implementation: `e84a3feead82e086d469aad06e6c592c59bfa64a`, branch `release/decision-exercises`. The PR head includes the evidence and browser-compatibility amendments. PR50 and other checkouts remain untouched. Main does not establish the deployed SHA.

## Open and inspect

In `/private/tmp/ir-decision-exercises`, run `npm run start -- --hostname 127.0.0.1 --port 3232` (or first `npm run build` if no build exists). Open [the candidate](http://127.0.0.1:3232/decisions). This task leaves that local production server running.

[Walkthrough with synthetic result links](walkthrough.html) · [compact contact sheet](contact-sheet.png). Try one complete exercise, an unchanged or withheld path, then one Foundation and AI result. All pictured results, stored compatibility records and exercise choices are synthetic. Opening a synthetic shared result does not inject answer evidence or a fabricated completion into an ordinary profile.

Public candidate routes: `/decisions`, `/decisions/who-gets-to-verify`, `/decisions/who-gets-access`. Cases and AI Governance provide modest entries. Chinese requests use the existing translation-unavailable page (HTTP200, no English exercise controls), not invented translation parity. `/dev/result-payoff` still returns404 in production. A normal production preview can serve the public routes; it cannot serve the guarded experiment. The existing Vercel integration automatically created a [PR preview](https://ir-worldview-app-git-release-decision-68b4ce-yippy141s-projects.vercel.app) after draft PR52 opened; no manual deployment was initiated.

## What changes

The shared player gives a conditional readback of the actual arrangements **and independently stated reasons**. It handles withheld decisions, unexpressed reasons and an unaffected arrangement without false policy/reason equality. Editing the original submission clears the replay. Essential assumptions are visible; the complete transcript, claim IDs/provenance, references and fuller limits remain in disclosure. National→custodian with timely→equal retains an external check while discussing reciprocity; custodian→custodian retains reciprocal custodian access. Enclave→hosted is not blanket rejection of criticism; enclave→weights accepts irreversible distribution outside developer admission control. Same arrangement with changed reasons explicitly records both facts.

Both v3 English publication records reuse the same typed content shape, player, diagrams, submission lifecycle and readback registry. The second decision changes who admits external evaluators, holding capability/misuse evidence and admitted teams’ rights fixed. It offers a different institutional question without requiring a baseline. No third issue or schedule is promised. A future ordinary publication link could contain only the canonical content slug; no publication account, campaign or identifier is configured.

Real Foundation results use their actual resolved names and equal-weight canonical blend marks. Only supported Shi/Concert shapes receive a one-time fresh-completion enhancement; shared, refreshed, unsupported, no-JS, reduced-motion and print views retain static marks. AI retains exact version-resolved issued labels and encoded alternatives; an exact model tie is disclosed without replacing the saved label through array order. Midpoint-distance quantities are accurately named. The synthetic Stewardship proposal and narrowly applicable example prose remain development-only; no universal answer synthesis is claimed.

## Foundation separation and compatibility

New ordinary full extensions use additive `baselineExtended`: 42 extension items, 56 with the 14 carried-forward core items. All12 `validation` items remain source-faithful in the bank and old `fullExtended` form (54/68); eight explicitly name the U.S./America. No country substitutions or research intake were introduced. Saved old drafts offer explicit continuation with unchanged totals or a confirmed restart. Missing core evidence blocks extension completion.

`foundationBaselineForm` records bank2/scorer2/structural4, administration1, exact item order and **common-item-only** comparison status. Existing v5 result `qs` and v7 session fields accept the additive form; old encoded values still resolve. Aggregate cohort/replay allowlists recognize the distinct form without new events or endpoints. Labels and approved Chinese chrome reflect the new counts. The calculation dispatch uses the existing extended calibration unchanged.

512 deterministic scored records, both modes and ranked choices, produce3072 equality comparisons against low/high/mixed validation answers, with omitted-validation results as the oracle. This proves mechanical equality, not human equivalence under changed burden or priming. The preserved legacy robustness diagnostic still exercises its old forms:3372 vectors/266177 answer perturbations. Its five numerical CSV/fixture outputs match the historical bytes exactly. Current digests are in [legacy-form-robustness/current-run.json](legacy-form-robustness/current-run.json); historical research files remain untouched. New-form evidence is the separate scoped test.

## Sources and finite semantic findings

Freshly opened: [official INF text](https://www.reaganlibrary.gov/archives/speech/treaty-between-united-states-america-and-union-soviet-socialist-republics), ArticleIX and XI1–3/inspection protocol; [GovAI report](https://cdn.governance.ai/Open-Sourcing_Highly_Capable_Foundation_Models_2023_GovAI.pdf), §4.1.3 pp19–20 and §4.2.3 p25; [publisher record](https://www.governance.ai/research-paper/open-sourcing-highly-capable-foundation-models); [arXiv record](https://arxiv.org/abs/2311.09227); [Kertzer appendix](https://jkertzer.sites.fas.harvard.edu/Research_files/YourMorals%20Appendix%20012714.pdf), printed pp3–4. Five distinct URLs, three underlying works; duplicate independent rechecks are not extra sources. GovAI publication is29September2023; the observed arXiv page also reports29September2023. The old failed treaty URLs remain recorded in the experiment. Treaty hardware precedent does not establish fictional AI monitoring feasibility. Project banks are internal editorial material.

[item-review.csv](item-review.csv) has605 explicit mode/form/locale rows:297 current public variants,108 saved-full exposures and200 legacy mode exposures. It includes actual prompts, clarifications, options, signals, actors, tasks, source locators and dispositions.143 current English items and68 effective Chinese records were read; historical banks were inventoried for replay, not re-authored. The original Kertzer study randomized20 items; this fixed12-item subset is not equivalent administration. A PDF screenshot fetch failed; text inspection succeeded.

Finite versioned repair candidates remain: ten material Chinese scope differences (especially intervention direction and basing versus economic severance); explanation/prescription mismatches including `gray_zone_sabotage`; accountability polarity in `an_tradeoff_ceasefire_settlement.hold_accountability_line`; separate risk forecast/policy and capacity/oversight meanings. These are documented candidates, not measured defect rates or silent permission to change issued weights.

The four PR51 witnesses remain unchanged: (7,7)/(1,1)/(4,4) all core geopolitics4 and identical complete Standard final3.7; one reachable independent-update permutation changes5.5→5.9 while retaining Precautionary Steward; oversight midpoint deviation1 differs from exact comparison term−1.2 (risk-horizon term2.16); Security’s explanation prompt still offers Publish/Create/Prioritize/Hold actions. No claim that every mean is invalid, or that a classification change was witnessed.

## Coverage, verification and boundaries

[surface-coverage.csv](surface-coverage.csv) records162 finite URL targets (118 readable/redirect targets and44 locale boundaries) and439 embedded records. All finite targets rendered;832 embedded assertions passed, including complete semantic map lists. Sixteen AI readings appear in static Atlas subsets; twelve required two synthetic result routes.201 source/reading records were inventoried, including118 distinct linked URLs; this is not118 external sources opened. The record distinguishes DOM assertions, captured regional visual checks, local-state requirements and actual link journeys. Infinite payload/state space is represented by the declared matrix, not exhausted. Regional desktop/mobile default/expanded inspections cover41 template families and164 representative capture regions, not every full page.188 capture entries represented187 files; one Wendt/Mearsheimer filename collision is explicitly corrected with a separately inspected Wendt capture. Three actual journeys of3/4/5 links passed; other depths describe the observed link graph, not replayed journeys.

Existing Batch B defects: decorative hero pseudo-elements cause1476px document width at1440 on six routes; the open Cases mobile menu causes770px at390. Temporarily removing the decorative pseudo-elements in browser memory eliminated the desktop overflow. No global CSS was changed. An initially unstyled `/current` view came from blocking the same server’s localhost alias; the corrected styled default passes390px. Populated reviews absent from initial crawl captures are covered by the supplemental synthetic-state tests, not retroactively claimed as inspected.

Passed: typecheck; lint; strict public-copy check; production build; full663-test unit suite;13 production Chromium exercise/result tests;16 preserved-experiment Chromium tests; four additional public state tests. The existing124-case Chromium suite also passed across116 cases in the full local run and eight targeted cases (four repaired plus four production-only cache/print checks). Tests cover20 Foundation identities, six AI labels/ties, all six targeted forms, old/full/shared/invalid outputs, both module/AI modes and reviews, single/multiple-domain legacy profiles and all six Perspective result roles. Both episodes retain800 combination coverage; valid recomputed applicability counterexamples and exact old replay fixtures pass. New public strings are explicitly included in the active copy inventory. The existing evidence writer refreshed only `artifacts/evidence/current-summary.*`; audit algorithms and historical numerical evidence were not weakened.

Initial failures: disk exhaustion interrupted cache/scratch writes; only this task’s generated Next cache was reclaimed. Restricted Turbopack build stalled and was interrupted; the normal build passed with local process permission. Obsolete name-hierarchy assertions and stale current evidence were corrected. The first GitHub run passed validation/unit/typecheck/build and failed18 browser assertions for the superseded result presentation. Those assertions now test canonical names, equal176px blend marks, the new AI hero and accessible disclosed metadata. Local custom-port failures were fixed by using Playwright’s baseURL; image metadata still must resolve to a real PNG. Three changed Chinese views have new release-specific Darwin/Linux baselines; all six images were inspected. Linux images came from the synthetic CI artifact at4128e46 (run34082697313), not a locally claimed Linux capture. Historical snapshots remain unchanged. CI run34085125180 passed the full663-unit and124-browser suites, validation, copy/evidence checks, lint, typecheck and build at7b5c75a. The existing CI browser job now also invokes the17 public exercise/result/state checks; its final workflow run is pending at this record’s commit. Failure uploads include its synthetic captures and privacy check. A test expected a long Perspective name where the existing renderer uses the short label; corrected expectation passed. WebKit was unavailable (`webkit-2311/pw_run.sh` missing); no install or actual Safari/device test is claimed. A fresh independent reviewer returned ACCEPT for the bounded code, not audience retention or psychometric validity.

New exercise inputs remain page-memory only: no profile reads, application persistence, answer-bearing requests/URLs, live model call or analytics event. Existing questionnaire/profile storage and telemetry are unchanged apart from recognizing the authorized distinct form. Browser tests block external/API writes and use isolated synthetic storage. Ordinary hosting metadata is not claimed absent. Existing unrelated telemetry attempts observed during the wider crawl were blocked, not activated or collected.

Owner decision: accept or reject this candidate’s public experience on the usual browser. Recommendation: continue to that decision, then Batch B’s reading hierarchy/layout and shared-menu repairs. Keep the stable baseline and use distinct episodes for novelty; locale and response-task changes need explicit new-version rationale. No recruitment, retention estimate or new research backend is a prerequisite to inspecting this candidate.

## Exact change boundary

[verification.json](verification.json) lists every modified source, test and evidence path. Protected-path comparison against the unchanged base has four authorized additive form files: `lib/quiz-schema.ts`, `lib/types.ts`, `lib/share.ts`, `lib/scoring/v2-calibration.ts`. No bank wording/signals, scoring arithmetic/calibration values, canonical names/mark registry, storage keys, APIs, database, telemetry modules, root/global design, dependencies or lockfiles changed. Existing form validators, replay and cohort labels recognize the new administration; historical experiment and research artifacts have zero diff. Shared implementation extraction and scoped result CSS are the remaining production changes.

## PR52 bounded repair — R1–R4

This appendix supersedes the earlier menu/overflow and unavailable-WebKit dispositions; the observations above remain history. No Batch B work, merge, manual deployment or collection is authorized by this repair. Verified reviewed head: `7d43f093651c8753df9e694ba891de6a7ff031d7`; unchanged base: `da96b7e9b51426ad2bfb0ab0cc9d0ad4bf248a79`. Repair implementation: `2c39896424d6e714fbfd4c7ff8a21691ba690c73`. The later documentation commit is deliberately separate from the tested code commit. Current CI belongs to the exact PR head, available in [PR52 checks](https://github.com/yippy141/ir-worldview-app/pull/52/checks), not to an unknown hash embedded in its own commit.

The original temporary worktree directory was absent. The same verified branch was attached at `/private/tmp/ir-pr52-release-repair`; no branch pointer, main checkout, other agent's changes or unrelated worktree registration was moved or removed. Run `cd /private/tmp/ir-pr52-release-repair` then `npm run start -- --hostname 127.0.0.1 --port 3232` (build first if needed). The local candidate is [here](http://127.0.0.1:3232/decisions); the existing integration's [candidate preview](https://ir-worldview-app-git-release-decision-68b4ce-yippy141s-projects.vercel.app/decisions) updates after pushing.

**R1 — reproduced and repaired.** [Before observations](repair/before-observations.json) record `/cases`, `/decisions`, `/about` and `/zh/cases` at320/390/768. English390 opened to770px: the in-flow `.mobile-nav`/`.mobile-nav-sheet` were734.219px wide (`min-width:auto` on the flex item, no maximum), shrinking the brand to zero. The sheet now positions against `.header-inner`, outside intrinsic flex sizing, with `width:min(28rem,100%)`, `min-width:0`, `max-width:100%` and bounded vertical scrolling. English sheet widths are280/350px at320/390; Chinese292/362px. All tested document extents equal the viewport. At768 the existing desktop More disclosure applies. Every destination, full label, language control and focus outline remains reachable; Tab leaves the non-modal disclosure, Escape returns to its summary. [Geometry320](repair/menu-geometry-320.json), [390](repair/menu-geometry-390.json), [768](repair/menu-geometry-768.json). The six reported desktop routes reproduced1476px at1440, default and expanded. Moving their decorative owner's `::before` right edge from−4% to0 produces1440px in every witness: [geometry](repair/desktop-geometry.json). No root overflow masking or map resizing.

**R2 — registry distinction and recovery.** `sc1` is tier extended despite its core scoring block. Its existing incomplete-extension test is retained and accurately named; the historical `screenshots/1440-missing-core.png` is a mislabeled extension artifact, not proof of a tier-core omission. The new [extension capture](repair/regression/1440-missing-extension.png) replaces that claim. Registry-confirmed `sc2` is tier core. [Before recovery](repair/before-core-recovery.json) showed generation correctly disabled with all42 extension answers present, but no route to the missing core question. Review now exposes that missing row and its ordinary Edit action. A navigation-only repair flag displays core questions while retaining the saved baseline/targeted form. All seven current extended paths recover, reload, edit and generate the exact expected payload only after restoration; no incomplete result/evidence/share is emitted. [Core recovery](repair/core-recovery.json). The extracted existing completeness rule covers112 omissions (14 core items × baseline, legacy full and six targeted forms). The512-record/3072 equality test and old replay remain unchanged.

**R3 — actual keyboard journey.** Separate from retained activation tests, bounded native traversal reaches Menu/More, links, Escape/close, radio groups and reasons, both submissions, the conditional readback, Back and Reset at390/1440. Stage changes focus the actual new heading with a visible outline. No test focus assignment or positive tabindex. [Narrow path](repair/keyboard-390.json), [desktop path](repair/keyboard-1440.json). macOS WebKit's default includes links with Option+Tab; Chromium uses Tab/Shift+Tab. This engine distinction is explicit, not a focus workaround in application code.

**R4 — consultation and exit.** The automated pre-repair source click navigated the original tab and cleared its choices. External links now announce a new tab and use `_blank` plus `noopener noreferrer`; exact publisher-URL mocks prove separate navigation, no added payload/identifier and retained original selections. Internal source links explicitly announce departure and clearing. Reset, Close, Next and ordinary departure still clear. WebKit additionally reproduced browser restoration of checked radios after Back; `autocomplete=off` on these ephemeral inputs prevents that restoration. [Consultation/request/storage assertions](repair/source-consultation.json). No storage key, API, analytics or persistence was added. Publisher responses were mocked for deterministic navigation checks, not freshly source-verified.

**Executed checks.** Typecheck, lint, strict copy (zero blocking), evidence byte check, production build and diff check passed. Final local suites:664 unit,128 existing Chromium,21 public Chromium,16 experiment Chromium, all passed; bounded WebKit8/8 after one successful installation. The four characterization witnesses,800 authored episode combinations, applicability negatives and old replay pass. Initial test failures are retained in verification: an edit/reload race in the new test, development-only Next debug session storage, WebKit's default link traversal, WebKit radio restoration, and the experiment's superseded h1-focus expectation. Production privacy assertions were not relaxed. Real Safari/device use is not claimed. These checks establish the repaired paths, not whole-product owner acceptance or audience retention.

**Screenshots inspected.** [Cases320 before](repair/before-menu-cases-320.png) / [after](repair/after-menu-cases-320.png); [Cases390 before](repair/before-menu-cases-390.png) / [after](repair/after-menu-cases-390.png); Chinese390, all six `repair/after-desktop-*.png`, [missing core](repair/missing-tier-core-baseline.png), the extension capture, [restored result](repair/restored-core-result.png), [keyboard result390](repair/keyboard-result-390.png) and [access source consultation](repair/source-consultation-who-gets-access.png) were visually inspected. Other generated captures, including Decisions before/after320/390 and the regression directories, were assertion-tested/captured without a claim of individual visual inspection. Files are committed through this PR, not solely temporary local evidence.

[Exact repair and protected-surface diff](repair/protection-diff.json). The only global CSS scope is the R1 selectors above; episode CSS only adds the focused stage heading to the existing scroll margin. The candidate's earlier additive form/compatibility/chrome changes remain visible in the base comparison and are not misreported as new repair changes. This amendment has zero diff in issued banks, scoring/calibration, canonical names/marks, localization meaning, payload/replay, storage contracts, APIs, telemetry/data modules, homepage, fonts or dependencies. Existing adapted/not-equivalent Chinese status remains.

**Five-minute owner path:** open the candidate on your usual browser; at a narrow width open Menu, scroll to the language controls, close and use Escape. Open Who gets to verify; choose national/timely, then custodian/equal and read the finding. Open a source and return to the still-selected exercise tab. Try Back/Reset, then Next to Who gets access. On desktop, check `/references` at1440 for horizontal movement. This is a short experience check, not a recruitment gate.

No additional blocker identified in this repair scope. Continue to the owner's candidate decision. After that check and merge, Batch B starts from new main: **adjudicate the existing ten material Chinese semantic findings in [the ledger](item-review.csv)**, preserving their substantive status; do not commission another inventory or silently treat matching IDs/signals as meaning equivalence.
