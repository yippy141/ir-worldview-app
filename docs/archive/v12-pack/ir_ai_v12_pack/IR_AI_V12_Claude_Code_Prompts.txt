PROMPT 0 — AUDIT AND FILE MAP ONLY
Read AGENTS.md, plans/V12_EXEC_PLAN.md, plans/V12_SOURCE_OF_TRUTH.md, the latest localhost PDF notes if present, and the existing touched files for Profile, module results, module entry pages, AI atlas, AI results, and shared visual primitives. Do not change app code yet. Create or update plans/V12_UI_AUDIT.md with: (1) current blockers on Profile payoff, module clarity, AI Atlas depth, and trust signaling, (2) a keep / reduce / replace map for each touched surface, and (3) a files-to-touch list. Treat current local code and localhost snapshot as the source of truth, not production. Stop after writing the audit and summarizing the proposed file list.

PROMPT 1 — PROFILE PAYOFF SURGERY
Refactor Profile so that when 2 or more saved layers exist, the page becomes pattern-first rather than Foundation-family-first. Requirements: use the nearest Atlas-style integrated pattern as the main headline; keep the closest modeled Foundation family as a subtitle only; above the fold must contain one short summary paragraph, max three chips (stable thread, biggest shift, AI present/absent), one dominant comparison visual, and one clear CTA row. Remove repeated stable/shift/reasoning sections above the fold. Demote or collapse full module re-summaries, AI re-statements, and appendix-like detail. If the current overlay/spine visual implies direct rescores on a shared measured axis, replace or relabel it so it clearly reads as relative pull / directional change inside this model rather than a fresh measurement. Keep the serious editorial style. Do not change scoring, payloads, routes, or taxonomy. Run npm run lint, npm run test, and npm run build, then stop and summarize files changed and any remaining rough edges.

PROMPT 2 — MODULE ENTRY AND RESULT CLARITY PASS
Improve only the Security and Technology module entry pages and module result pages. Entry pages: shorten the pre-question ramp so they feel less like briefing packets. Keep one paragraph on what the module tests, one compact Foundation linkage block, the mode choice, and one collapsed or calmer “how to read these cases” area. Visible lanes, perspective coverage, and long instruction blocks should be collapsed, trimmed, or visually demoted. The “What it does not claim” content must remain, but it should no longer sit in a visually awkward side box; integrate it into a cleaner notes block or collapsed scope/limits section that matches the rest of the layout. Result pages: replace the current “Selected framings” dump with a clearer “Decisive calls” or similarly named grouped section showing only the top 4 to 6 most informative choices plus one sentence explaining what they imply. Keep the full evidence log, but collapse it by default. Do not change scoring, question IDs, payloads, or routes. Run npm run lint, npm run test, and npm run build, then stop and summarize.

PROMPT 3 — AI ATLAS CLICK-THROUGH + AI FIELD GUIDE
Do not add a new scored module. Keep AI taxonomy, scoring, and payloads unchanged. Make AI easier to browse and learn from. Requirements: keep /ai/atlas as the browse surface; add clickable archetype detail pages at /ai/atlas/[id] (or the smallest route structure that preserves current browse meaning); each archetype detail page should include a short definition, compact fingerprint, nearest neighbors, strongest critique, starting readings, and a clear route back to the AI result, AI atlas, and AI field guide. Add a dedicated /ai/field-guide page that explains what the AI module measures, what it does not fully cover, the eight axes in plain English, where adjacent archetypes part ways, and a shelf for under-modeled perspectives such as labor/automation, surveillance/platform power, environmental/compute externalities, Global South dependence/sovereignty, Chinese official framings, and open-source / anti-incumbency politics. If useful, add one small manually curated “current debates to watch” rail driven by a typed content file, but do not build a live feed, scraper, or new dependency. Add clear navigation between AI result, AI atlas, AI field guide, and Profile. Run npm run lint, npm run test, and npm run build, then stop and summarize.

PROMPT 4 — TOUCHED-SURFACES RESPONSIVE AND GRAMMAR PASS ONLY
Do a final visual pass only on the surfaces changed in V12: Profile, Security module entry/result, Technology module entry/result, AI Atlas browse/detail, AI Field Guide, and any shared primitives those surfaces use. Goal: make them feel like one editorial system with stronger hierarchy and calmer spacing, not a new redesign. Preserve the current typography language. Remove any leftover same-weight box clutter on those pages. Make sure the new Profile top, module notes blocks, Decisive calls blocks, and AI browse/detail pages stack cleanly around 390px width and still export acceptably to PDF. Do not reopen the homepage, IR taxonomy, scoring, or payload logic. Run npm run lint, npm run test, and npm run build, then stop and list the remaining rough edges.
