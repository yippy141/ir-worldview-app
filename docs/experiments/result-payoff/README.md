# Result payoff and recurring decisions

**Non-shipping experiment.** Owner-authorized exception for this route only. The production freeze in `docs/roadmap/POST_V23_6_PRODUCT_RESET_STATUS.md` remains in force. No advisor-proposed name is adopted. The homepage, menu, globe and issued sigils are preserved. No merge or deployment is authorized.

Base: fetched `origin/main` at `c3e74edbe32e921b7a346d00e12afe45073cd956`. Branch: `experiment/result-payoff`; isolated worktree: `/private/tmp/ir-result-payoff`. PR50 is not the base. Claude's uncommitted prototype in the owner's checkout was inspected read-only and left untouched.

## Open locally

```sh
cd /private/tmp/ir-result-payoff
npm run dev -- --hostname 127.0.0.1 --port 3227
```

Open [the experiment](http://127.0.0.1:3227/dev/result-payoff). The development selector exposes Foundation, AI, missing/legacy, episode-first and returning-reader examples. Direct episode links are [verification](http://127.0.0.1:3227/dev/result-payoff?episode=verify) and [access](http://127.0.0.1:3227/dev/result-payoff?episode=access). The returning fixture opens access immediately, with a synthetic September 1 completion marker.

The existing `NODE_ENV === "production"` guard returns 404, including on production-mode Vercel previews. There is no sitemap or public-navigation exposure. Dependencies were copied from the existing checkout for local execution; no dependency or lockfile changes.

## What this demonstrates

Foundation uses a newly authored, complete 14-item core response set. Its registered reading is **Shi (勢)–Concert**, with equal-weight registered sigils. Military preparation, monitoring-based agreements and avoiding overextension coexist. Institutionalism and realism remain live interpretations: the registered core-calibrated family scores are 0.81 and 0.51. The rival argument concerns whether monitoring sustains a bargain or depends on the balance of power sustaining it. It is not an extreme answer substitution or a claimed second choice.

AI leads with the specific combination: severe frontier-risk concern, pausing a potentially dangerous release, and retaining monitored outside testing. **Precautionary Steward** is secondary vocabulary; **Democratic Guardrailist** is the nearest modeled alternative. Separate rivalry and coordination responses retain information the current average discards. Fresh optional answers stay separate and never re-score the instrument.

Either episode works without a baseline. Verification asks what inspection arrangement to accept when reciprocal national access becomes asymmetric. Access asks who controls admission to qualified evaluation, holding capability and misuse evidence fixed. The second is worth testing as a return invitation because it poses a different unresolved decision: external scrutiny may exist yet remain dependent on developer permission. This is a product hypothesis, not observed retention.

The built visual treatment retains Spectral display headlines and Libre Franklin functional type on the existing navy field with brass actions and instrument accents. Foundation reuses the registered sigils. Each complete reading or episode places one substantive diagram area beside the main reading column; completed episodes pair original and changed arrangements within that area. Mobile collapses to a linear page without a sticky rail. Native, initially collapsed disclosures separately expose answer evidence, editorial interpretation, exact arithmetic, source scope and fixture provenance.

## Four characterizations

The eight focused tests in `tests/result-payoff.test.mts` extend existing diagnostic coverage without adding a diagnostic framework or changing any scorer.

| Property | Observed finding and limit |
| --- | --- |
| AI information loss | Bank 3/scorer 2, complete Standard records with `(gp1,gp2)` of `(7,7)`, `(1,1)` or `(4,4)` all give core geopolitics **4**. Holding all other answers fixed gives identical full results, including final geopolitics **3.7**. `gp1` concerns rivalry as a durable constraint; reverse-coded `gp2` prioritizes coordination over advantage. Endorsing both is not indecision or a preferred answer. |
| Incremental clamping | Complete valid Standard witness: every core answer scores 7 after reversal; `capabilityThreshold=C`, all five other roots `A`. Original geopolitics: **5.5**. Swapping the first two independent updates: **5.9**. Official generation equals the diagnostic replay in the original order. The original `7 + 0.4` clips before `−0.5`; swapping retains 0.4 more. Other axes and the selected archetype remain unchanged; archetype scores change. This is one reachable axis witness, not a prevalence estimate or an observed classification-change witness. Branching narratives were not permuted. |
| “What is doing the work” | Current AI bars are signed midpoint distance `(score−4)/3`, ordered by magnitude. They are not leading-minus-runner terms. Here oversight has maximum midpoint distance but contributes **−1.20** to Precautionary Steward minus Democratic Guardrailist; risk horizon contributes **+2.16**. Exact version-registry profiles give a rounded **0.87** gap, reconciled with all terms and rounding residual. Both registered AI versions are tested. |
| Security semantics | Current bank 5/scorer 2 `gray_zone_sabotage` is an explanation card asking what a rival is testing. Options begin “Publish”, “Create”, “Prioritize”, “Hold”: policy actions partly stand in for explanations. This confounds the question's meaning; it does not establish how often readers misinterpret it. Issued bank untouched. |

## Claims, provenance and evidence

`experiments/result-payoff/evidence.ts` defines the small typed claim contract: ID, instrument/version/form/copy provenance, references, kind, supported and unsupported conclusions, and reader text. Each rendered reading has a native disclosure. All exact calculation terms remain inspectable.

| Claim group | Evidence |
| --- | --- |
| `foundation-cooperation-with-preparation` | Exact `sc2=6`, `in2=7`, `rs2=6`; full synthetic form and binding disclosed. |
| `foundation-exact-comparison`, `foundation-rival-explanation` | Registered V2 core calibration and profiles; the rival also references the reviewed INF case argument. |
| `ai-contain-capability-preserve-scrutiny` | `rh1=6`, `capabilityThreshold=A`, `openWeights=B`, their issued wording and full form. |
| `ai-rivalry-and-coordination`, `ai-exact-comparison`, `ai-rival-accountability` | Separate `gp1/gp2`, exact version-specific arithmetic, and a labeled editorial challenge. |
| `verify/*`, `access/*` | Submitted arrangement and principal reason in each condition; direct observation, bounded interpretation and proposed question remain distinct. |
| Optional reflections | Only explicitly submitted fresh answers. Never interpreted as official results. |

Foundation provenance: structural version 4, bank 2, scorer 2, core form with its registered calibration, English copy 1. AI: bank 3, scorer 2, Standard, English copy 1. Experiment copy: 1. Synthetic SHA-256 bindings include the exact response entries, provenance and generated result. This local fixture contract does not replace or weaken the production local-evidence lookup. Missing, altered, stale or score-identical-but-differently-answered records fail closed. No owner PDFs, answers or browser profile are loaded. Exact ties retain all leaders; a narrow gap carries no stable-identity claim.

## Sources and editorial reuse

Verification preserves the repository's `security-arms-control-verification` record and `inf_src_01`, plus AI `multilateralVerification`. The State Department archive returned a technical-difficulties page on recheck; the source remains the existing reviewed record, not newly verified treaty text. Its hardware precedent does **not** establish AI verification feasibility. Actors, technical reach, refusal rules, timelines and costs are explicit fictional assumptions. Both episodes remain **draft**.

Access reuses AI `openWeights`, Technology `open_weight_models`, and the existing Seger reading. [Seger et al., 2023, §§4.1.3 and 4.2.3](https://cdn.governance.ai/Open-Sourcing_Highly_Capable_Foundation_Models_2023_GovAI.pdf) supports structured researcher access and independent mediation of admission. The specific veto contrast is an illustrative design, not a construct validated by that paper. Downloaded weights cannot be recalled. Completion links directly to the relevant existing Futures entries, Gatekeeper and Open Frontier; their historical signal copy is not adopted as fresh intelligence here.

**Recipe for the next issue:** author one `Episode` record. Change actor, assumptions, options with accepted costs, principal reasons, one condition, bounded readback and sources. Reuse `EpisodePlayer`, `Arrangement`, explicit submission, Back/Reset, source disclosures and completion text assembly unchanged. Existing `CurrentCaseOption`, `CurrentCaseSource` and publication-status types are reused. Human work remains checking source scope, serious comparable options, unchanged facts, role equivalence, wording effects and overinterpretation. Review both changed and unchanged paths on mobile. The planning allowance is **90–120 minutes of owner editorial judgment**, not a measured authoring time; new research-heavy cases need a separate estimate.

After a future publication decision, an ordinary canonical episode URL could be sent through the owner's existing publication. It should contain only a public content slug, with no answer payload or app-generated person identifier. No canonical public episode route, release schedule, reminders or subscription service exists in this experiment.

## Measurement proposal (not activated)

Later events: episode opened, first decision submitted, interpretation reached, and another episode completed on a later date. The existing telemetry adapter permits named events with `caseId`, `routeCategory`, `deviceClass`, `referrerCategory` and `returningAgeBucket`; episode names are not registered. A later decision could permit a public episode ID and coarse operational categories only. Do not collect answers, reasons, political labels, raw result URLs, demographics or a stable cross-domain identifier.

Repeat-use events are not verified unique people. An anonymous completion counter cannot recover a person-level retention cohort. Newsletter clicks are not episode completions; local history misses cleared storage and cross-device use. Vercel custom-event availability and property limits depend on the project plan, which is unverified. No automatic pageviews are added. Future collection needs a narrow operational/privacy decision and a synthetic staging canary confirming a write; HTTP 202 alone is insufficient.

An optional consented reader group could confirm participation across issues without supplying answers. Keep contact information and participation confirmations separate from political responses. Neither that register nor a research backend is implemented.

## Verification and screenshot index

Final checks: `npm run typecheck`, `npm run lint`, `npm run test` (652 passed, including exact old replay fixtures), `npm run build`, and `./node_modules/.bin/playwright test --config playwright.result-payoff.config.ts` (9 passed). The static design detector reported no findings. Independent finish review resolved all five material fixes; its disposition was “ship”, explicitly limited to this dev-only demonstration, not production. See [verification.json](verification.json) for initial failures and their resolutions. Chromium checks are not real-device Safari coverage or evidence of reader comprehension or retention. Screenshots contain only synthetic fixtures and fictional exercise selections. No historical release artifacts are overwritten. [Exact changed paths](changed-paths.txt) list every source, test and screenshot. The sole shared-code change is the exact development-route registration in `lib/site-shell.ts`. Protected-path diff is empty: issued banks, scorers, calibrations, codecs/payloads/replay, stable IDs, homepage/global design, APIs/database, telemetry, dependencies/lockfiles, public-copy audit and frozen evidence/roadmap artifacts.

All images are in [`screenshots/`](screenshots/). Prefixes `1440` and `390` mean 1440×900 and 390×844 browser viewports. Files ending `-full`, and episode sequences, capture the full linear page at that width.

- `{width}-foundation.png`, `{width}-ai.png`, `{width}-missing.png`: first viewports. Their `-full.png` companions preserve complete evidence acts.
- `{width}-verify-a-original.png` → `-b-replay.png` → `-c-finding.png`: verification sequence.
- `{width}-access-a-original.png` → `-b-replay.png` → `-c-finding.png`: access sequence.
- `{width}-returning.png`: clearly synthetic prior completion and direct access entry.
- `768-reduced-motion.png`; `390-foundation-no-js.png`: accessibility evidence.

Next.js 16 development tooling itself caches initial server-debug chunks under `__next_debug_channel:*` in **sessionStorage**. Browser checks identify that existing framework behavior and compare byte-identical before/after checkpoints across each exercise. Experiment inputs cause no writes; localStorage, cookies and answer-bearing requests remain empty. Do not describe the development runtime as making zero storage writes.

## Specific note for the Astra PM

Claude's prototype improves connected entry and saved-record access, but its introduction still starts with First Principles. This experiment tests a different question: whether one worthwhile decision can earn a return before an assessment. The two can coexist without renaming the product or replacing the distinctive homepage.

The more consequential issue is semantic, not naming: the AI average collapses different positions, and the Security card partly substitutes recommendations for explanations. Better prose cannot recover missing evidence. Preserve separate answers where available; offer a bounded follow-up when they are not. Do not use similar scores across these instruments as a cross-domain relationship.

Recommendation: **continue the non-shipping experience hypothesis**, with owner review of the two institutional contrasts before any reader pilot. The next useful evidence would be whether a reader can explain the displayed finding and voluntarily chooses a later decision, not whether these synthetic tests pass. This recommendation does not lift the production freeze or authorize recruitment.
